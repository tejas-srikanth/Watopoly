#include <iostream>
#include <fstream>
using namespace std;
void saveGame(std::string filename="watopoly.txt"){
    ofstream myfile; 
    myfile.open(filename); // opens the file
    /*if( !myfile ) { // file couldn't be opened
        cerr << "Error: file could not be opened" << endl;
        exit(1);
    }*/

    myfile << players.size() << endl;

    for (auto p : players) {
        myfile<<p->getName()<<" "<<p->getPiece()<<" "<<getTimsCups(p)<<" "<<p->getBal()<<" ";
        int pos = p->getPos();
        myfile<<pos;
        if (pos == 30 && p->getJail()) {
            myfile<<" 1 "<<getJailRounds(p);
        } else {
            myfile<<" 0";
        }
    }

    for (auto s : property) {
        if(s->getOwner()) {
            myfile<<s->getOwner()->getName()<<" "<<s->getImprovement();
        } else {
            myfile<<"BANK 0";
        }
        
    }
}